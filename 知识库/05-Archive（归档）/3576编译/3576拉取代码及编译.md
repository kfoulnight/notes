
repo init -u  git@xxxxx  -b develop -m default.xml  
	repo init 建立.repo -u指定位置 -b版本 -m哪个配置  
	mainfast.xml 指向当前使用的清单文件的软链接
repo sync 

 repo forall -c 'git checkout DTOOLS_COMMON 2>/dev/null || echo "跳过 $REPO_PATH（无此分支）"'

 repo forall -c 'echo "$REPO_PATH : $(git branch --show-current)"'

source build/envsetup.sh
lunch
./build.sh -ABUCKuop
make -j8